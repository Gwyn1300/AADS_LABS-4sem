#include "function.h"
#include "experiment.h"

int main(){
    experement();
    set a;
    setRandomNumberFilling(a, 10);
    set b;
    setRandomNumberFilling(b, 10);
    chalange(a,b);
    return 0;
}